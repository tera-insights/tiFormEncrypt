/// <reference path="../typings/index.d.ts" />
/// <reference path="../src/base64.ts" />

import { expect } from 'chai';

describe('base64', () => {
    describe('base64toUint8Array', () => {
        it('should correctly encode basic example', () => {
            expect(tiForms.base64toUint8Array('')).to.be.equal(1);
        })
    });


})


